{
  "name": "namoshi-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for Namoshi Name Service on Citrea",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020 namoshi-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 namoshi-subgraph --version-label v1.0.0",
    "remove-local": "graph remove --node http://localhost:8020 namoshi-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.71.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}